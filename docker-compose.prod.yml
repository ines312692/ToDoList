version: '3.8'

services:
  frontend:
    image: todo-frontend:latest
    ports:
      - "8080:80"
    networks:
      - todo-network

  backend:
    image: todo-backend:latest
    ports:
      - "3000:3000"
    networks:
      - todo-network

networks:
  todo-network:
    external: true
