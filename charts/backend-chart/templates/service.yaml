apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-backend.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "todo-backend.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
